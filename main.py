import threading

if __name__ == "__main__":
    try:
        # bot_thread = threading.Thread(
        #     target=...  # заменя
        # )
        # bot_thread.daemon = True
        # bot_thread.start()
        pass
    except KeyboardInterrupt:
        print("Бот выключен")
